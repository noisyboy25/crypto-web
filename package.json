{
  "scripts": {
    "start": "cd server && yarn start ",
    "dev:client": "cd client && yarn start",
    "dev:server": "cd server && yarn dev",
    "dev": "run-p -l dev:*"
  },
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
